// These settings override both User and Default settings for the zorgmode syntax
{
	"color_scheme": "Packages/zorgmode/Color Scheme/orgmode.tmTheme",

	"link_transform_rules": [
		{
			"url_prefix": "gh:",
			"replacement": "https://github.com/%s"
		}
	]
}
